<!DOCTYPE html>
<html>
<head>
    <title>CicloData</title>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 0;
            height: 100vh;
            position: relative;
            overflow: hidden;
        }

        .sidebar-container {
            position: fixed;
            top: 0;
            left: 0;
            width: 250px;
            height: 100%;
            background-color: rgba(0, 0, 0, 0.7);
            padding-top: 20px;
            z-index: 1000;
            border-radius: 15px; /* Borda arredondada */
        }

        .sidebar a {
            padding: 10px 20px;
            text-decoration: none;
            font-size: 1.2rem;
            color: white;
            display: block;
            transition: background-color 0.3s;
            text-shadow: 1px 1px 2px black; /* Sombra no texto */
        }

        .sidebar a:hover {
            background-color: rgba(0, 0, 0, 0.9);
        }

        .sidebar .navbar-brand {
            font-size: 1.5rem;
            font-weight: bold;
            color: white;
            margin-left: 20px;
            text-shadow: 1px 1px 2px black; /* Sombra no texto */
        }
    </style>
</head>
<body>
    <div class="sidebar-container">
        <div class="sidebar">
            <a class="navbar-brand" href="/">Menu</a>
            <a href="/users">Usuarios Cadastrados</a>
            <a href="/users/new">Cadastrar Usuarios</a>
            <a href="/bikes">Produtos</a>
            <a href="/bikes/new">Cadastrar Produtos</a>
        </div>
    </div>
</body>
</html>
